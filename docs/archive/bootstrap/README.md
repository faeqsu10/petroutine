# Bootstrap Notes

이 디렉터리에는 프로젝트 초기 방향을 잡을 때 사용한 자료를 모아둔다. 현재 코드베이스의 동작을 설명하는 문서가 아니라, 출발점과 의사결정 맥락을 복원할 때 참고하는 기록이다.

여기 있는 파일은 현재 규칙의 단일 진실원본이 아니다. 실행 절차와 최신 구조는 `README.md`, `docs/README.md`, `CLAUDE.md`, `tasks/`를 기준으로 본다.
